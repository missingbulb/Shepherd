# Version history

Records for `packs/claudinite-dashboard/pack.mjs`'s `version` field, one row per bump. The
rows below are version-numbered comments that used to sit beside `version:` in the manifest,
moved here verbatim; nothing earlier than the first of them was backfilled. Every bump from
here forward adds its own row.

| Version | Date | What changed |
|---|---|---|
| 60902.13 | 2026-09-02 | The roster models one precondition form: `precondition_signals` is no longer lifted, and `has_precondition` is derived from the conditions alone (#1617). |
| 60902.12 | 2026-09-02 | The repo page's top block and the Work board (#1606), and one explore panel per status (#1607). The block is the fleet's ledger one member deep — the same bands and tracks, three figures per column instead of four, and a machine whose heartbeat is one square per HOUR rather than per member, beside CI on main. `repo-ledger.mjs` adds the three questions only a repo can ask: the queue's own outcome WORDS (an obsolete share, and `none` above zero meaning items closed without converging at all), this repo's corpus weight against the fleet mean, and the per-task expand — what each task closed, what it cost, and what it kept parking on. THE WORK BOARD replaces the Work table as the default view, with the three tables behind it as tabs: `board.mjs` builds the lanes as connected components of the `Blocked-by`/`Ends-when`/`Closes` graph — never one task and never one kind, because the edge is the finding — places each mark from the field it read, and draws a lane BROKEN where a plain issue nobody is scheduled to close holds it. `board-view.mjs` draws the identity's mark vocabulary, form before colour throughout. The Scheduled group is a task × day grid where a failure park lands on its own task's row, so a hatched cell followed by filled ones is the record disagreeing with the roster's held-lane claim, on one line. `explore.mjs` writes the six panels: every field names its source, a field the page cannot read says *not read* rather than being omitted, and every panel ends in one imperative — with the converge command spelled as `converge-item.mjs`'s own usage block spells it. A `<section>`'s inherited bottom margin was putting 104 px of air inside a block budgeted at 450. |
| 60902.11 | 2026-09-02 | The fleet page's top block is the ledger sheet the redesign specifies (#1605): one ruled surface with a stub column and five bands — Start here as a warm slip, The machine's five cells with a per-member heartbeat, the Got/Cost/Speed ledger on fixed tracks with its totals under a double rule and a per-member expand, the pulse, and a band naming where the grid begins. It replaced the benefits tiles, the glance tiles and two of the four activity charts; each fact those carried moved to where it is acted on rather than being dropped. New `fleet-ledger.mjs` decides every figure — the window against the previous window, the three states a figure can be in (a number, *not recorded*, *unpriced*), and each figure's own *bad when* — and new `sheet.mjs` renders the identity's marks, one function per mark. The identity's palette is scoped to the sheet rather than `:root` while the panels below still read the old tokens; it carries the full theme pattern, redefined twice for dark. The wake strip states an absence rather than drawing an empty 24 hours: a task's anchor lives in a declaration the fleet page does not fetch, and an empty strip reads as the serious verdict. |
| 60902.10 | 2026-09-02 | The page-side reads the redesign's figures rest on (#1604), all of them reductions over responses the page already fetches — the viewer makes no new request. The issues listing's PR projection now keeps a **merged** PR inside a 14-day window, with its merge date and the issue it closes parsed before the body is dropped, so the lead-time series covers the days the fold has not reached; a complete open listing no longer discards them. New `pricing.mjs` prices a window's `tokensByModel` against the deployment's `rates` table — per model, per counter, cache creation falling back to the input rate — and keeps three states apart: a number, *unpriced* (no table, and the key is named), and a priced sum with an unpriced remainder that is never folded into it. The scheduler heartbeat is topped up from the fold's hour tier, so a member whose last scheduler run is older than one page of runs stops reading as one that has never run — the critical verdict. `fleetCorpus` gains the fleet mean of rule tokens per session, averaged over members rather than pooled, with the member count beside it. And `wakeStrip` buckets the roster's next asks into the coming 24 UTC hours, placing a held task at now rather than dropping it. The cache's stored-shape version moves with the PR projection. |
| 60902.9 | 2026-09-02 | The redesign spec's `data-sources.md` states the fold's `parks` vocabulary as the imported `PARK_KINDS` rather than a re-spelled list, and the per-pack rule-token facet without thousands separators — the facets are joined into one comma-separated line, so a comma has to stay the segment's own terminator for the fold's parse to know where the split ends (#1603). |
| 60902.8 | 2026-09-02 | The fleet view gains **What the corpus is doing across the fleet**: workload tiles this week against last, the `work` and `world` check scopes side by side, the findings per rule, the skills that load against the skills mounted and never loaded, and one usage row per member — all derived from the usage folds the sweep already reads, so no request is added. Page-only; nothing in a member changes shape and there is no migration (#1581). |
| 60902.7 | 2026-09-02 | The Pages stub declares `defaults.run.shell: bash`. Its cache-key step pipes, and GitHub's implicit shell is `bash -e` without pipefail, so a half-failed pipeline would have produced a key that silently matches the wrong build — `gha/run-pipefail`, blocking, and invisible here because no `gha/*` check scans a stub (#1595). |
| 60902.6 | 2026-09-02 | The Cloudflare token step names the fields the form actually asks for — a token name, Account Resources and Zone Resources — rather than leaving three blanks to guess, and copying the token becomes its own step. The claim that the *Edit Cloudflare Workers* template grants Workers Scripts · Edit "and nothing else" is withdrawn: it carries a zone-scoped permission too, which is why the form asks at all. What is true, and is what the docs now say, is that the deploy calls only `/accounts/<id>/workers/…`, so the zone half is never exercised (#1594). |
| 60902.5 | 2026-09-02 | The sign-in checklist is reordered into producer/consumer pairs — each value is pasted into its variable or secret in the box straight after the one that mints it — and the two Cloudflare credentials become four such steps rather than one line pointing at the deploy task. The install step carries the installations URL instead of naming the tab, and the notes say that the 404 it lands you on is expected — the redirect URI is registered so wildcard matching permits the pages beneath it, and sign-in returns to the dashboard's own URL (#1592). |
| 60902.4 | 2026-09-02 | The deploy task's client-secret name is `DASHBOARD_OAUTH_CLIENT_SECRET`. It was `GITHUB_OAUTH_CLIENT_SECRET`, which GitHub refuses to create — so no deployment can be carrying the old name and there is nothing to migrate (#1590). |
| 60902.3 | 2026-09-02 | The "Turning sign-in on" checklist matches the GitHub App registration form: the field it called **Callback URL** is **Redirect URI**, **Homepage URL** (which the form requires) is named beside it, and a step unticks **Webhook → Active**, which arrives ticked and blocks submission until a Webhook URL is given — this App receives no webhooks. README-only; nothing in a member changes shape and there is no migration (#1590). |
| 60902.2 | 2026-09-02 | `deploy-oauth-exchange` converts to `preconditions: ['none']` — a manual lever's item exists only because somebody created one, and that IS the request. The task roster's declaration reader learns the new form beside the legacy one, and reads `['none']` as no gate rather than as a gate that always passes (#1578). |
| 60902.1 | 2026-09-02 | The retired `merged-pr`/`open-pr` outcome spellings are swept out of the pack's own test fixtures and the CSS comment illustrating hyphenated declaration values (#1470). |
| 60830.1 | 2026-08-30 | The `deploy-oauth-exchange` task puts the sign-in endpoint live and verifies the deployed URL answers, so turning sign-in on is a run rather than a paste; `oauth-exchange.example.mjs` is now `oauth-exchange.mjs` (it is deployed, not illustrated) and the declaration gains the optional `allowedOrigins` and `workerName` keys. Its Cloudflare account id is a repository variable rather than a declared secret. The sign-in pair move out of the declaration into the repository variables `CLAUDINITE_DASHBOARD_CLIENT_ID` and `CLAUDINITE_DASHBOARD_EXCHANGE_URL`, read by one module the build and the deploy share; a declaration still carrying `clientId`/`exchangeUrl` is read as the fallback and reported, so no configured deployment loses its button. The seeded Pages workflow gains those two `vars` lines and folds them into its deploy cache key, and an existing member needs that stub re-seeded (a PR into its `.github/`) before a variable reaches its build. A member gains a manual task; nothing runs on a cadence and no existing key changes meaning, so the bump carries no migration record (#942). |
| 60827.2 | 2026-08-27 | The `fleet-digest` task, its two checks (`digest-plain-text`, `dated-fixture-collision`) and the morning-brief panel that read its output are retired — with no writer there is no series to read, so the `digestsRepo`/`digestsPath`/`digest` config keys and the `FLEET_GITHUB_TOKEN` handover step go with them. A member's standing `claudinite-dashboard/fleet-digest` item closes itself through the queue's orphan reap, and the config reader spreads an unknown key through untouched, so the bump carries no migration record (#1392). |
| 60827.1 | 2026-08-27 | The open work set is read live (`state=open`, conditional on every page) and the TTL'd history pages are reconciled against it, so a closed item stops being reported as open work (#1389). The page's parked state is its own display key, `PARKED`, rather than the `needs-human` label it used to borrow: nothing writes that label any more, so a page comparing against it would have counted no parks at all (#1119). Two changes claimed this number (#1482), so a member that converged between the two merges took only one of them; both are in every mount from the next version on. |
| 60824.3 | 2026-08-24 | `fleet-digest`'s `schedule_after` drops the retired `claudinite-fleet-sheepdog/fleet-usage` — the destructive tail of #1158 (#1166). |
| 60824.2 | 2026-08-24 | The page reads the queue's vocabulary and anchor math through the `claudinite-tasks` pack's published `shared-code/`, declared as a `requires` (#1317). |
| 60823.2 | 2026-08-23 | The page reads the settings-file names from the engine's browser-pure half, so its import graph carries no `node:` builtin and the dashboard loads again (#1286). Page-only; no migration. |
| 60823.1 | 2026-08-23 | Reads a member's settings under either name, judges mount freshness from the installed versions alone, and drops the converged tile the deleted converge datetime used to feed (#1252). |
| 60822.2 | 2026-08-22 | `fleet-digest`'s `daily+1h` offset becomes `schedule_after:` naming the three sheepdog sweeps — the offset expressed the wish and enforced nothing, and a sweep running long simply overran it. Naming another pack's tasks is inert where that pack is not declared. Uses the renamed `schedule_after` field (was `after`). The human-work estimate is priced per park kind — 15 min a broken park, 10 an action, 3 a decision, and an approval by its PR's size at 1 min per 200 lines (#1247). Recovery-rule trips leave the figure: they wear no label and the janitor clears them, so they are reported beside it like the scheduler faults already were. Page-only for a member: nothing in its tree changes shape and there is no migration. Two changes claimed this number (#1482), so a member that converged between the two merges took only one of them; both are in every mount from the next version on. |
| 7 | — | The fleet-digest task arrives from the claudinite-fleet-sheepdog pack, with its two checks. A declaring repo gains a daily task; nothing in a member is rewritten, and the task still reads an enforcer's existing `claudinite-fleet-sheepdog` config as its legacy source, so the bump carries no migration record. |
| 8 | — | The FLEET_GITHUB_TOKEN the digest needs is stated once, in its own fleet-token.mjs, and rendered into the missing-secret message, the adoption step and a 403's hint — additive, no migration (#1030). |
| 9 | — | Adoption hands over the sign-in decision as well as the Pages setting — prose and a handover entry, so a member gains a checkbox and nothing else changes. |
| 10 | — | The page carries a favicon — a file the mount has to deliver, so the version moves; nothing in a member's tree changes shape and there is no migration. |
| 11 | — | Mount freshness judged on stamped versions against the canon's live ones (never ref/updated), and the scheduler panels re-derived for the standing-item model — next asks, roll records, triage-split parks. Page-only; no migration. |
| 12 | — | The workflow-practice neighbour is git-github now that github-actions collapsed into it (#1079). |
| 60820.1 | — | Versions become date-anchored (#1100) — the counter this list is written in retires here, and every pack in the canon restarts from the same day. |
| 60820.2 | — | Fleet-digest's machine-issue filter learns the schedule board's `[claudinite-schedule]` title (#1115). |
| 60821.1 | — | Item state, triage and parked counts are decoded from the label vocabulary rather than matched literally, so a member's items read the same whichever engine filed them (#1119). |
| 60821.2 | — | Both pages are rebuilt around the repos' own usage folds (#1158). The repo page gains at-a-glance tiles, one work table with three views in place of the roster and queue tables, an hourly runs graph and a month of what the corpus is doing; the fleet page reads each member's usage file and enumerates its roster from an `owner` as the viewer rather than from a stored list. Which dashboard a deployment builds is decided by that config's shape, so nothing is asked at adoption. Page-only: a member gains panels and nothing in its tree changes shape. |
| 60821.3 | — | Which dashboard a deployment builds is DECLARED, not inferred from whether a roster source happens to be present. `mode` is the one config key with no default: the build refuses to publish without it, and refuses a mode that contradicts the rest of the config. A member that had let silence mean "repo" is stamped with `mode: "repo"` by this pack's migration record. |
| 60821.4 | 2026-08-21 | This pack's inline version-history comments moved out of `pack.mjs` into this file. |
| 60822.1 | — | Any pack can contribute its own key figures to both pages (#1194). A pack ships `dashboard.json` naming its widgets; values come from its own `.claudinite/local/dashboard/<pack>.GENERATED.json` or from `latest-release`/`repo-stars`, which the page already reads. The repo page gains a *What the packs report* region, and each fleet member gains a subrow of mini-cards. Stars moves out of the dashboard's own code into `git-github`'s descriptor, so a member not declaring that pack no longer shows a star count. Page-only for a member: nothing in its tree changes shape and there is no migration. |
| 60822.3 | 2026-08-22 | Both pages lead with the one thing to do (#1249). A new `next-work.mjs` ranks every fault the page already found and names one — worst first, an issue ahead of a repo-level fault, longest-stuck ahead of freshest — rendered as a card above every panel that reports, priced by what that one park asks of a person — one term of the attention estimate's own sum, and no figure at all where that estimate does not reach. The fleet's morning brief moves up beside it and gains a day picker back through the series, in place of the two fixed cards below the fold. Page-only for a member: nothing in its tree changes shape and there is no migration. |
| 60822.4 | 2026-08-23 | The manifest stops restating its own tree (#1246): `id`, `prose`, `badge`, `skills`, `worldRules` and `workRules` are resolved from the pack directory and an absent `detect`/`marker` means no fingerprint. Coded rules move into `worldRules/`/`workRules/` and tests into `test/`, which no vendor set ships. `minEngineVersion` rises to the engine release that reads all of it. |
| 60824.1 | 2026-08-24 | The fleet sweep reads horizontally — every member through one pass before any member starts the next, identity then attention then depth then pack cards then commit graphs (#1311). A budget that runs out mid-load now costs the same panel on every row rather than the last members entirely. Page-only; no migration. |
